# Security

Do not expose authentication data, reference audio, generated job directories, or other private media in issues, logs, screenshots, or commits.

The Codex edition stores local configuration and job data in `.cs-board-codex/`, which is ignored by Git. Report security issues privately to the repository maintainer rather than opening a public issue first.
