# Security

Do not expose authentication data, reference audio, generated job directories, or other private media in issues, logs, screenshots, or commits.

VideoSketchIt stores local configuration and job data in `.videosketchit/`, which is ignored by Git. Older installations migrate data from `.cs-board-codex/` automatically. Report security issues privately to the repository maintainer rather than opening a public issue first.
